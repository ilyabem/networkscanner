# Зависимости проекта «Сканер и редактор топологии сети»
# Установка: pip install -r requirements.txt

# Обнаружение хостов и сканирование портов
python-nmap>=0.7.1        # обёртка над системным nmap (nmap нужно установить отдельно!)

# ARP-сканирование и работа с сетевыми пакетами
scapy>=2.5.0              # требует Npcap (Windows) или root (Linux)

# Граф и layout
networkx>=3.0

# GUI
PyQt5>=5.15.0

# SNMP-опрос
pysnmp>=4.4.12

# Определение производителя по MAC
mac-vendor-lookup>=0.1.12

# Тестирование
pytest>=7.0
